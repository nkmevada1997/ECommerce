@model Country

@{
    ViewData["Title"] = "GetCountry";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card" style="margin:auto;">
            <div class="card-header bg-dark text-white text-center bg-dark" style="height:50px">
                <h5 class="mt-1">Country Details</h5>
            </div>
            <div class="card-body bg-dark text-light" style="border-top:3px solid white">
                <table class="table table-striped table-dark">
                    <tr>
                        <td width="20%">
                            <b>@Html.DisplayNameFor(country=> country.CountryName)</b>
                        </td>
                        <td>@Html.DisplayFor(country=> country.CountryName)</td>
                    </tr>

                    <tr>
                        <td>
                            <b>@Html.DisplayNameFor(country => country.CreatedDate)</b>
                        </td>
                        <td>@TimeZoneInfo.ConvertTimeFromUtc(@Model.CreatedDate,TimeZoneInfo.FindSystemTimeZoneById(TimeZoneInfo.Local.StandardName)).ToString("dd/MM/yyyy hh:mm:tt")</td>
                    </tr>
                </table>
                <div style="border-top:3px solid white">
                    <div class="mt-3 text-center">
                        <a href="@Url.Action("EditCountry", "Countries",new {countryId=Model.CountryId})" class="btn btn-warning">
                            <i class="fa fa-edit"></i>
                            <span>Edit Country</span>
                        </a>
                        <a href="@Url.Action("Index", "Countries")" class="btn btn-secondary">
                            <i class="fa fa-list"></i>
                            <span>Countries</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


