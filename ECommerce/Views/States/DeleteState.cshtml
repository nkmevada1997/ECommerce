@model State

@{
    ViewData["Title"] = "DeleteState";
}

<h3 class="text-center text-uppercase text-light">Delete Country</h3>
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card" style="margin:auto;">
            <div class="card-header bg-dark text-white text-center bg-dark">
                <h5 class="mt-1">Are you sure you want to delete this?</h5>
            </div>
            <div class="card-body bg-dark text-light" style="border-top:3px solid white" ">
                <table class="table table-striped table-dark">
                    <tr>
                        <td width="40%">
                            <b>@Html.DisplayNameFor(state=> state.StateName)</b>
                        </td>
                        <td>@Html.DisplayFor(state=> state.StateName)</td>
                    </tr>

                    <tr>
                        <td width="40%">
                            <b>@Html.DisplayNameFor(state=> state.Country.CountryName)</b>
                        </td>
                        <td>@Html.DisplayFor(state=> state.Country.CountryName)</td>
                    </tr>
                    <tr>
                        <td>
                            <b>@Html.DisplayNameFor(state=> state.CreatedDate)</b>
                        </td>
                        <td>@Model.CreatedDate.ToString("dd/MM/yyyyy hh:mm:tt")</td>
                    </tr>

                </table>
                @using (Html.BeginForm())
                {
                @Html.AntiForgeryToken()
                <div class="form-actions no-color" style="border-top:3px solid white">
                    <div class="mt-3 text-center">
                        <button type="submit" style="width:150px" class="btn btn-danger">
                            <i class="fa fa-trash-can"></i>
                            <span>Delete </span>
                        </button> |
                        <a href="@Url.Action("Index", "States")" class="btn btn-secondary ml-3">
                            <i class="fa fa-list"></i>
                            <span>States</span>
                        </a>
                    </div>
                </div>
                }
            </div>
        </div>
    </div>
</div>


