@{
    if (!string.IsNullOrEmpty(Context.Session.GetString("UserId")) && !string.IsNullOrEmpty(Context.Session.GetString("UserType"))
             && Context.Session.GetInt32("UserType") == 1)
    {
        Layout = "_AdminLayout";

    }
    else
    {
        Layout = "_Layout";
    }
}
