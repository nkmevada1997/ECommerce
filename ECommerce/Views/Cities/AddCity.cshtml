@model AddCityModel

@{
    ViewData["Title"] = "AddCity";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card" style="margin:auto;">
            <div class="card-header bg-dark text-white text-center">
                <h5 class="mt-1">Add State</h5>
            </div>
            <div class="card-body bg-dark text-light" style="border-top:3px solid white">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                @Html.LabelFor(city=> city.StateId)
                                @Html.DropDownListFor(x => x.StateId, new SelectList(ViewBag.StateItems, "StateId", "StateName"),"Select State",new { @class = "form-control"})
                                <span class="text-danger">@Html.ValidationMessageFor(city=> city.StateId)</span>
                                <br />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                @Html.LabelFor(city=> city.CityName)
                                @Html.TextBoxFor(city=> city.CityName, new { @class = "form-control" })
                                <span class="text-danger">@Html.ValidationMessageFor(city=> city.CityName)</span>
                                <br />
                            </div>
                        </div>
                    </div>

                    <div class="form-group" style="border-top:3px solid white">
                        <div class="mt-3 text-center">
                            <button type="submit" class="btn btn-primary" style="width:180px">
                                <i class="fa fa-plus"></i>
                                <span>Add City</span>
                            </button> |
                            <a href="@Url.Action("Index", "Cities")" class="btn btn-secondary">
                                <i class="fa fa-list"></i>
                                <span>Cities</span>
                            </a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

